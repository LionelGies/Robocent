<p>
  Select a Phone Number<br/>
  Subscribers can text a keyword to this number to subscribe to your text notifications,
  call this number to subscribe to your voice notifications and
  you may use it as a text comment line in which you can communicate with your customers!
</p>

<% if @user.twilio_phone_number.present? %>
  <p>
    Your Selected Number:
    <strong>
      <%= @user.twilio_phone_number.phone_number %>
    </strong>
  </p>
<% else %>

  <p>
    The area code in which you'd like a new incoming phone number.
    Any three digit, US area code is valid.
    We will provision a random phone number within this area code for you.
    You must include either this or the PhoneNumber parameter to have your POST succeed.
  </p>

  <%= fields_for @twilio_phone_number ||= TwilioPhoneNumber.new do |t| %>

    <div class="row alignleft">
      <label><strong>Enter Area Code</strong></label>
      <%= t.text_field :area_code, :class => "inputtext", :style => "width:150px;" %>
    </div>

    <div class="space"></div>
    <div class="space"></div>
    <div class="alignleft" style="padding-top:30px;">OR</div>
    <div class="space"></div>
    <div class="space"></div>

    <div class="row alignleft">
      <label><strong>Select Phone Number</strong></label>
      <%= t.select :phone_number, TwilioRequest::available_phone_numbers.collect{|n| [n.phone_number]}, {:prompt => "Select one" } %>
    </div>

    <div class="clear"></div>

  <% end %>

  <br/>
<% end %>

<p>
  What if I want to use a short code like 41257 instead?<br/>
  You will have that option on the dashboard once you create an account.
  For now, please select a phone number (they are free and you don't have to use it
  if you don't want to).
</p>


<%= hidden_field_tag :step_id, "2" %>

<div class="row alignleft">
  <%= link_to "BACK", register_user_path(:step_id => "1"), :class => "btn-submit btn-link" %>
</div>

<div class="space"></div>

<div class="row alignleft">
  <%= f.submit "NEXT", :class => "btn-submit", :id => "user_form_step_2_submit" %>
</div>

<div class="clear"></div>