<div class="g_6 contents_header">
  <h3 class="i_16_dashboard tab_label">Dashboard</h3>
</div>

<div class="g_12 separator"><span></span></div>

<%= render "shared/show_flash" %>

<div class="g_4">
  <div class="widget_header">
    <h4 class="widget_header_title wwIcon i_16_ui">Contacts</h4>
  </div>
  <div class="widget_contents">
    <div class="banner-box">
      <span class="highlight-number">
        <%= @total_contacts_count %>
      </span>
      <span class="small-text">
        ACTIVE<br/>CONTACTS
      </span>
      <div class="clear"></div>
    </div>
    <div class="contacts-stat">
      <%= pluralize(@text_contacts_count, "Texting Contacts") %>
      <br/>
      <%= pluralize(@call_contacts_count, "Calling Contacts") %>
    </div>
    <div class="link">
      <%= image_tag("dashboard/custom/add_contacts.png") %>
      <%= link_to "Add and update contacts", contacts_path %>
      <div class="clear"></div>
    </div>
  </div>
</div>
<div class="g_4"></div>
<div class="g_4">
  <div class="widget_header">
    <h4 class="widget_header_title wwIcon i_16_tabs">Billing Summary</h4>
  </div>
  <div class="widget_contents">
    <div class="banner-box">
      <div class="balance">
        <%= "$#{"%.3f" % current_user.current_balance}" %>
        Current Balance
      </div>
    </div>
    <div class="balance-stat">
      It will cost
      $<%= "%.3f" % @cost_for_text %>
      to send a text to your contacts<br/>
      It will cost
      $<%= "%.3f" % @cost_for_call %>
      to send a call to your contacts
    </div>
    <div class="link">
      <%= image_tag("dashboard/custom/add_contacts.png") %>
      <%= link_to "Add Funds to your Account", billing_path %>
      <div class="clear"></div>
    </div>
  </div>
</div>

<div class="g_12">
  <div class="widget_header wwOptions">
    <h4 class="widget_header_title wwIcon i_16_data">Recent Activity</h4>
    <div class="w_Options i_16_settings">
    </div>
  </div>
  <div class="widget_contents noPadding">
    <table class="datatable tables">
      <thead>
        <tr>
          <th>Date</th>
          <th>Call or Text</th>
          <th>List Sent to</th>
          <th>% of Completed</th>
          <th>Cost</th>
          <th>View Progress</th>
        </tr>
      </thead>
      <tbody>
        <% @text_messages.each do |text_msg| %>
          <tr>
            <td>
              <%= text_msg.created_at.in_time_zone(current_user.time_zone).strftime("%b %d, %Y %H:%M") %>
            </td>
            <td>
              <%= text_msg.number_of_recipients %>
            </td>
            <td>
              <% text_msg.lists.collect{|l| l.name }.each do |list_name| %>
                <%= list_name %> <br/>
              <% end %>
            </td>
            <td>100</td>
            <td>
              <%= "$%.3f" % text_msg.total_cost %>
            </td>
            <td>
              <%= link_to "view", "#" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<script type="text/javascript">
  $(document).ready(function(){
    $("table.datatable thead tr th:first").click();
  })
</script>